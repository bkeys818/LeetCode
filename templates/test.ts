import solution from '../src/__srcFilename__'

describe('__name__', () => {
    test.each<>(
        // prettier-ignore
        []
    )('case %#', (expected, ...args) => {
        expect(solution(...args)).toBe(expected)
    })
})
